import { Sheet } from "src/modals/Sheet";
import { getDocumentById } from "./firebase";

export const getSheetById = (sheetId: string): Sheet => {
    try {
        // getDocumentById("", sheetId)
    } catch (error) {
        
    }
    return null;
}